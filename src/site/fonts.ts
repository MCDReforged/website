import { Inter, Noto_Sans_SC } from "next/font/google"

export const fontInter = Inter({
  subsets: ["latin"],
  display: 'swap',
})

export const fontNotoSans = Noto_Sans_SC({
  subsets: ["latin"],
  display: 'swap',
})
